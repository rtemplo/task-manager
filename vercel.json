{
  "version": 2,
  "buildCommand": "tsc -p tsconfig.server.json && tsc -p tsconfig.api.json && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }]
}
